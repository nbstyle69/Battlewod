package expo.modules.realtimerecorder

// This class is no longer used.
// EGL context management is now handled directly by RecorderEngine
// which maintains a single shared EGL context for both preview and encoder surfaces.
